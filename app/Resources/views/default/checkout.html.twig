{% extends '::base.html.twig' %}

{% block body %}
    {{ form_errors(form) }}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <input type="submit" value="Next" />
    {{ form_rest(form) }}
{% endblock %}